@page
@model ResidentCare.Pages.Notes.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Note</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<div class="form-group">
                <label asp-for="Note.ResidentId" class="control-label"></label>
                <select asp-for="Note.ResidentId" class ="form-control" asp-items="ViewBag.ResidentId"></select>
            </div>*@

            <div class="form-group">
                <label asp-for="Note.Text" class="control-label"></label>
                <textarea asp-for="Note.Text" rows="6" class="form-control"></textarea>
                <span asp-validation-for="Note.Text" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
