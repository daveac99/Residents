@page
@model ResidentCare.Pages.Notes.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Note</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Note.NoteId" />
            <input type="hidden" asp-for="Note.ResidentId" />
            <input type="hidden" asp-for="ScreenSource" />
            @*<div class="form-group">
                <label asp-for="Note.ResidentId" class="control-label"></label>
                <select asp-for="Note.ResidentId" class="form-control" asp-items="ViewBag.ResidentId"></select>
                <span asp-validation-for="Note.ResidentId" class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="Note.Text" class="control-label"></label>
                <textarea asp-for="Note.Text" rows="6" class="form-control"></textarea>
                <span asp-validation-for="Note.Text" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>

    @*<a asp-page="/Residents/@Model.ScreenSource" asp-route-id="@Model.Note.ResidentId">Back to List</a>*@
    <a asp-page="/Residents/@Model.ScreenSource" asp-route-id="@Model.Note.ResidentId">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
